import React from 'react';
import classes from './Styles.module.scss'

export const Counter = () => {
    return (
        <div>
            <button className={classes.button}>123</button>
        </div>
    );
};
